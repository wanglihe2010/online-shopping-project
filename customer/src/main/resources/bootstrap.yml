---
# N.B. this is the default:
spring:
  cloud:
    config:
      uri: http://localhost:8888
  application:
    name: customer-service
server.port: 9193

management:
  endpoints:
    web:
      exposure:
        include: "*"