---
spring:
  cloud:
    config:
      uri: http://localhost:8888
  application:
    name: item-service
server:
  port: 9292

app:
  security:
    basic:
      enabled: false

management:
  endpoints:
    web:
      exposure:
        include: "*"