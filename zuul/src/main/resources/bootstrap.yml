---
spring:
  cloud:
    config:
      uri: http://localhost:8888
  application:
    name: zuul-gateway-server
server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: "*"
